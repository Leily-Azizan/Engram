#!/bin/bash

echo "===== Server health: $(hostname) ====="
echo "Uptime and load:"
uptime
echo "Memory:"
free -h
echo "Disk usage:"
df -h /
echo "Top 5 memory-user processes:"
ps aux --sort=-%mem | head -6
echo "Failed services:"
systemctl --failed
